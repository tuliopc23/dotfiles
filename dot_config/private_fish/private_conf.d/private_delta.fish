# Delta abbreviations
abbr -a gds "git diff --staged"
abbr -a gdh "git diff HEAD"

# Ensure man pages are colored if desired (optional, using bat usually, but just in case)
# set -x MANPAGER "sh -c 'col -bx | bat -l man -p'"
