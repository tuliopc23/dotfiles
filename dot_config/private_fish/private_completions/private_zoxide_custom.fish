# Completions for custom zoxide functions

# Completion for zr (zoxide remove)
complete -c zr -f -a '(zoxide query --list)'

# Completion for zq (zoxide query) 
complete -c zq -f

# Completion for zf (zoxide with tv)
complete -c zf -f
